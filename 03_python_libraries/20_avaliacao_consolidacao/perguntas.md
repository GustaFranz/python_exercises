# Avaliacao - Consolidacao (Fase 2) - Perguntas

> Responda antes de abrir gabarito.md

## Pergunta 1

Qual a diferenca entre `merge(how='inner')` e `merge(how='left')` no contexto de juntar 3 fontes de notas?

---

## Pergunta 2

Por que converter colunas com `.astype(float)` antes de calcular a media?

---

## Pergunta 3

Explique a formula de media ponderada do boletim passo a passo, incluindo `nota_projeto * 2`.

---

## Pergunta 4

Quando usar `.apply(lambda x: ...)` em vez de operacao direta na coluna? De um exemplo do boletim.

---

## Pergunta 5

Por que usar `encoding='utf-8-sig'` ao exportar CSV no Windows?

---

## Pergunta 6

O que `notas.groupby(['turma', 'aluno'])` produz no contexto de `gerar_boletins()`?

---

## Pergunta 7

Como calcular percentual de alunos em recuperacao usando apenas Pandas, sem loop?

---

## Pergunta 8 (cenario hipotetico)

Se `provas_longo.csv` nao tivesse registros de Bruno Costa, o que aconteceria apos merge inner com simulados que tem Bruno?

---

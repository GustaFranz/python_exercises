# 21 - Sorteio de um aluno

## Objetivo

Sortear aleatoriamente um aluno de uma lista.

## Enunciado

Sorteia um aluno entre os cadastrados para apagar o quadro.

## Resolucao (retorno 30/07)

Nomes entram por `input` (nao mais fixos no codigo). Funcoes `ler_alunos` + `sortear_aluno` com `random.choice`. `raise` se a lista ficar vazia.

## Como executar

```bash
cd "21_sorteio_um_aluno"
python main.py
```


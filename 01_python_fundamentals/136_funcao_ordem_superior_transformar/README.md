# 136 - Funcao de ordem superior: transformar com lambda interna

## Objetivo

Usar lambda dentro de uma funcao para transformar valores de uma lista com `map()`.

## Enunciado

Crie uma funcao que aplica um reajuste percentual aos precos usando lambda interna e `map()`.

## Como executar

```bash
cd "136_funcao_ordem_superior_transformar"
python main.py
```
